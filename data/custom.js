/**
 * This file will hold custom Javascript functions that can be called from 
 * the triggers in the data files. 
 * 
 * You can use the function register to call a function by name. 
 * 
 * For example: F(foo) would look through this document to find 
 * the foo function and then call it.
 */
var aoidos_custom = {
    /**
     * Type alle your functions here using this function as a template:
     */
    testFunction : function(){
        //If this function is called by a condition, return either true or false
        //If this function is a trigger, act on something
    }
}